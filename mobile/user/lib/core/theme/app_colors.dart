import 'package:flutter/material.dart';

class AppColors {
  static const Color primaryPink = Color(0xFFE04595); // Approximated from "neo" / buttons
  static const Color primaryBlue = Color(0xFF38B6FF); // Approximated from "Pet"
  static const Color backgroundLightPurple = Color(0xFFEEF0FA); // Approximated background
  static const Color white = Colors.white;
  static const Color black = Colors.black;
  static const Color grey = Colors.grey;
  static const Color textDark = Color(0xFF2C2C2C); // Approximated dark text
}
