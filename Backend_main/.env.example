# Database Settings
# (Set DATABASE_URL to your Neon PostgreSQL connection string)
DATABASE_URL=postgresql://neondb_owner:password@ep-gentle-feather-ao7hg5ec.c-2.ap-southeast-1.aws.neon.tech/neondb?sslmode=require

# Optional legacy database connection fallback params
DB_USERNAME=
DB_PASSWORD=
DB_NAME=
DB_PORT=
DB_HOST=

# Application Public Base URL
# Set this to your Railway deployment hostname in production (e.g. https://xxx.up.railway.app)
# This is used for prefixing local file upload URLs (vet certifications, profile pictures).
BASE_URL=http://localhost:8000

# Azure Storage (Optional / Legacy Backup Uploads)
AZURE_STORAGE_CONNECTION_STRING=
AZURE_CONTAINER_NAME=images
